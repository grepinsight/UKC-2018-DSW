---
output: 
  html_document: 
    number_sections: yes
---
```{r}
library(ggplot2)
```

# What do you see when you run this?

```{r eval=FALSE}
ggplot(iris)
```

# Plot the histogram of Sepal.Length

```{r}
ggplot(iris, aes(x=Sepal.Length)) + 
  geom_histogram()
```

* What is the overall shape?
* What is the max value of Sepal.Length?
* How many peaks are there?



# Plot the histogram of Sepal.Width

```{r}
ggplot(iris, aes(x=Sepal.Width)) + 
  geom_histogram()
```

